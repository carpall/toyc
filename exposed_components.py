import xlexer, preprocessor, parser, compiler_utils, data

COMPONENTS_MODULES = {
  'lexer': xlexer,
  'preprocessor': preprocessor,
  'parser': parser,
  'compiler_utils': compiler_utils,
  'data': data
}